
const GridSingle = ({product}) => {
    const { name, description, price,period,tag,features,icon } = product;
    return (
        <div>
            
        </div>
    );
};

export default GridSingle;